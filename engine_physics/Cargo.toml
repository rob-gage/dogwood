# Copyright Rob Gage 2026

[package]
name = "engine_physics"
version = "0.0.0"
edition = "2024"
license.workspace = true

[dependencies]
engine_compute = { version = "0.0.0", path = "../engine_compute" }
